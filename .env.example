# Variables de entorno para la aplicación

# URL de conexión a la base de datos MySQL
# Formato: mysql://username:password@hostname:port/database
DATABASE_URL=mysql://username:password@hostname:port/database

# Clave secreta para firmar tokens JWT
JWT_SECRET=your_jwt_secret_key

# Tiempo de expiración de tokens JWT (opcional, por defecto 1h)
# JWT_EXPIRATION=1h

# URL del frontend para CORS (opcional, por defecto http://localhost:3000)
# FRONTEND_URL=http://localhost:3000

# Entorno de ejecución (development, production)
# NODE_ENV=development

# Puerto para el servidor (opcional, por defecto 5000)
# PORT=5000